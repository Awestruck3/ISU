
public class Knight extends ChessUnit {
    public Knight(){
        number = 2;
        rule = "Can only move in L shape (Two tiles in one axis, one tile on other axis)/";
        abilities = "May jump over friendly units";
    }
}
